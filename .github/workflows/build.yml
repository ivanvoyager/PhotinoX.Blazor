name: Build

on:
  workflow_dispatch: {}
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore --nologo

      - name: Build
        run: dotnet build -c Release --no-restore --nologo -warnaserror

      - name: Test
        run: dotnet test --configuration Release --no-build

      - name: Pack (nupkg + snupkg)
        run: dotnet pack Photino.Blazor/PhotinoX.Blazor.csproj -c Release --no-build -o artifacts --nologo /p:IncludeSymbols=true /p:SymbolPackageFormat=snupkg /p:VersionSuffix="pre.${{ github.run_number }}"

      - name: Upload nupkg
        uses: actions/upload-artifact@v7
        with:
          name: photinox-blazor-nupkg
          path: artifacts/*.nupkg

      - name: Upload snupkg
        uses: actions/upload-artifact@v7
        with:
          name: photinox-blazor-snupkg
          path: artifacts/*.snupkg